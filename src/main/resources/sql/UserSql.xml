<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap
        PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
        "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="user">

    <typeAlias alias="user" type="com.wiloon.enlab.entity.User"/>

    <select id="findByNameAndPassword" resultClass="user">
		select
		user.id as userId,user.username as userName,user.password
		from tbl_user user
		where user.username=#userName# and user.password=#password#
	</select>

</sqlMap>